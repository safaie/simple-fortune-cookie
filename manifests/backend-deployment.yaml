apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      name: backend
  template:
    metadata:
      labels:
        name: backend
    spec:
      nodeSelector:
        agentpool: linux
      containers:
        - name: backend
          image: ghcr.io/safaie/backend:latest
          ports:
            - containerPort: 9000
              name: backend
              protocol: TCP
          env:
            - name: REDIS_DNS
              value: redis
