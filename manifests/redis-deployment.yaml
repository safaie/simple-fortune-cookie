apiVersion: apps/v1
kind: Deployment
metadata:
  name: cookie-redis
  labels:
    app: cookie-redis
spec:
  selector:
    matchLabels:
      app: cookie-redis
  replicas: 1        
  template:          
    metadata:
      labels:
        app: cookie-redis
    spec:
      nodeSelector:
        agentpool: linux
      containers:
      - name: cookie-redis
        image: redis:7.0-alpine
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
          limits:
            cpu: 500m
            memory: 512Mi
        ports:
        - containerPort: 6379

