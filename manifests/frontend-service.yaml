apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    name: frontend-svc
spec:
  selector:
    name: frontend-svc
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
